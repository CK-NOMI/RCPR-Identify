# 代码修改日志 2026-06-09

## 16:36:57 推理阶段允许缺失 groundtruth

- 修改文件：`dataset.py`
- 修改目的：支持只有图片、没有 GT 的推理输入目录，例如 `datasets/AQUA20_grouped/image/shark_2image`。
- 修改时间：2026-06-09 16:36:57
- 验证情况：已用缺失 `groundtruth/shark_2image` 的临时映射执行推理，模型成功处理 2 张图片，输出到 `predictions/ours_aqua20_shark_2image_u1_k20/CoCA/shark_2image`；Stage-1 输出到 `predictions/ours_aqua20_shark_2image_u1_k20/CoCA_stage1/shark_2image`。

### 修改前

```python
missing_gt_dirs = []
for rel_dir in class_list:
    cur_gt_dir = gt_root if rel_dir == '.' else os.path.join(gt_root, rel_dir)
    if not os.path.isdir(cur_gt_dir):
        missing_gt_dirs.append(rel_dir)
if missing_gt_dirs:
    preview = ', '.join(missing_gt_dirs[:5])
    raise RuntimeError(f'Missing groundtruth group dirs ({len(missing_gt_dirs)}): {preview}')
```

```python
gt = Image.open(gt_paths[idx]).convert('L')
```

### 修改后

```python
if is_train:
    missing_gt_dirs = []
    for rel_dir in class_list:
        cur_gt_dir = gt_root if rel_dir == '.' else os.path.join(gt_root, rel_dir)
        if not os.path.isdir(cur_gt_dir):
            missing_gt_dirs.append(rel_dir)
    if missing_gt_dirs:
        preview = ', '.join(missing_gt_dirs[:5])
        raise RuntimeError(f'Missing groundtruth group dirs ({len(missing_gt_dirs)}): {preview}')
```

```python
if os.path.isfile(gt_paths[idx]):
    gt = Image.open(gt_paths[idx]).convert('L')
elif self.is_train:
    raise RuntimeError(f'Missing groundtruth file: {gt_paths[idx]}')
else:
    gt = Image.new('L', img.size, 0)
```

## 17:10:31 前端封装多图共显著识别上传交互

- 修改文件：`前端/code.html`
- 修改目的：把多图共显著物体识别的上传、预览、提交和结果展示封装到前端页面。
- 修改内容摘要：将原静态页面重写为 UTF-8 中文完整 HTML；保留 TailwindCSS 风格、顶部导航、上传卡、流程步骤、右侧优势卡和统计卡；新增原生 JS 上传逻辑、`/api/cosod` POST 请求、`files[]` 表单字段、加载态、错误提示、结果渲染函数。
- 修改时间：2026-06-09 17:10:31
- 验证情况：通过本地静态服务打开 `http://127.0.0.1:8765/code.html`，页面标题、上传区、空结果区正常；0 张图片点击“开始体验”会提示至少上传 2 张；控制台无脚本错误，仅有 Tailwind CDN 生产环境提示。

### 修改前

```html
<button class="bg-primary hover:bg-primary-hover text-white px-6 py-3 rounded-xl font-medium transition-colors shadow-md shadow-primary/20 flex items-center gap-2">
  ...
  寮€濮嬩綋楠?
</button>
```

```html
<div class="flex-grow flex flex-col items-center justify-center text-center">
  ...
  <h3 class="text-xl font-medium text-content mb-2">鏆傛棤璇嗗埆缁撴灉</h3>
</div>
```

### 修改后

```html
<button id="startBtn" class="bg-primary hover:bg-primary-hover disabled:bg-content-muted disabled:cursor-not-allowed text-white px-6 py-3 rounded-xl font-medium transition-colors shadow-md shadow-primary/20 flex items-center gap-2">
  ...
  开始体验
</button>
```

```javascript
const API_URL = '/api/cosod';
...
selectedFiles.forEach(file => formData.append('files[]', file));
...
const response = await fetch(API_URL, {
  method: 'POST',
  body: formData
});
```

```javascript
function renderResults(data) {
  const results = getResultList(data);
  ...
}
```

## 17:23:51 接入 `/api/cosod` 后端服务

- 修改文件：`cosod_backend.py`
- 修改文件：`test.py`
- 修改文件：`utils.py`
- 修改目的：让前端上传图片后可以通过 `/api/cosod` 调用现有 SCoSPARC 推理流程，并返回原图、Mask、叠加效果 URL。
- 修改内容摘要：新增标准库 HTTP 后端；扩展 `test.py` 的 CoCA 自定义图片/GT 路径；扩展 `utils.py` 的 CRF 图片读取路径；后端负责上传保存、推理调用、Mask 复制、叠加图生成和 JSON 返回。
- 修改时间：2026-06-09 17:23:51
- 验证情况：`python -m py_compile cosod_backend.py test.py utils.py` 通过；`scosparc` 环境中 `import cosod_backend` 成功；后端启动后 `/api/health` 返回 `{"ok": true}`；浏览器打开 `http://127.0.0.1:8765/code.html` 正常。本次未主动提交图片触发模型推理。

### `cosod_backend.py` 修改前

```text
文件不存在。
```

### `cosod_backend.py` 修改后

```python
class CosodHandler(BaseHTTPRequestHandler):
    ...
    def do_POST(self):
        parsed = urlparse(self.path)
        if parsed.path != '/api/cosod':
            self.send_error(404)
            return
        ...
        file_items = form['files[]'] if 'files[]' in form else []
        ...
        with INFER_LOCK:
            log_path = _run_inference(job_id, image_root, gt_root, model_folder)
        results = _build_results(saved_images, job_id, model_folder)
```

### `test.py` 修改前

```python
if testset == 'CoCA':
    test_img_path = './datasets/CoCA/image/'
    test_gt_path = './datasets/CoCA/groundtruth/'
```

### `test.py` 修改后

```python
custom_img_path = str(getattr(args, 'custom_img_path', '') or '').strip()
custom_gt_path = str(getattr(args, 'custom_gt_path', '') or '').strip()
if custom_img_path:
    os.environ['COSOD_CUSTOM_IMAGE_ROOT'] = custom_img_path
...
if testset == 'CoCA':
    test_img_path = custom_img_path if custom_img_path else './datasets/CoCA/image/'
    test_gt_path = custom_gt_path if custom_gt_path else './datasets/CoCA/groundtruth/'
```

### `utils.py` 修改前

```python
if dataset == 'CoCA':
    img = cv2.imread('./datasets/CoCA/image/'+paths[i][0][:-4]+'.jpg')
```

### `utils.py` 修改后

```python
if dataset == 'CoCA':
    custom_image_root = os.environ.get('COSOD_CUSTOM_IMAGE_ROOT', '').strip()
    if custom_image_root:
        img = cv2.imread(os.path.join(custom_image_root, paths[i][0][:-4]+'.jpg'))
    else:
        img = cv2.imread('./datasets/CoCA/image/'+paths[i][0][:-4]+'.jpg')
```

## 17:44:28 修复前端识别完成后结果区域被隐藏

- 修改文件：`前端/code.html`
- 修改目的：修复后端已经生成结果，但前端“识别结果”区域仍为空白的问题。
- 修改内容摘要：修改 `setLoading()` 状态切换逻辑，避免 `setLoading(false)` 隐藏 `resultGrid`；新增 `getCurrentFiles()`，提交时在内部 `selectedFiles` 为空时兜底读取 `fileInput.files`。
- 修改时间：2026-06-09 17:44:28
- 验证情况：检查真实后端任务 `20260609_173621_0c3976a2`，确认 `web_outputs` 已生成 2 张原图、2 张 Mask、2 张叠加图；静态检查确认 `setLoading()` 只在进入加载状态时隐藏结果列表。

### 修改前

```javascript
function setLoading(isLoading) {
  ...
  loadingState.classList.toggle('hidden', !isLoading);
  emptyState.classList.add('hidden');
  resultGrid.classList.add('hidden');
  if (isLoading) updateStats(selectedFiles.length, 0, 50);
}
```

```javascript
async function submitCosod() {
  if (selectedFiles.length < 2) {
    ...
  }
  const formData = new FormData();
  selectedFiles.forEach(file => formData.append('files[]', file));
}
```

### 修改后

```javascript
function setLoading(isLoading) {
  ...
  loadingState.classList.toggle('hidden', !isLoading);
  if (isLoading) {
    emptyState.classList.add('hidden');
    resultGrid.classList.add('hidden');
    updateStats(getCurrentFiles().length, 0, 50);
  }
}
```

```javascript
function getCurrentFiles() {
  if (selectedFiles.length > 0) return selectedFiles;
  if (fileInput.files && fileInput.files.length > 0) return Array.from(fileInput.files);
  return [];
}
```

```javascript
async function submitCosod() {
  const filesToUpload = getCurrentFiles();
  if (filesToUpload.length < 2) {
    ...
  }
  const formData = new FormData();
  filesToUpload.forEach(file => formData.append('files[]', file));
}
```

## 19:02:15 封装一键启动和停止脚本

- 修改文件：`start_cosod_web.ps1`
- 修改文件：`start_cosod_web.bat`
- 修改文件：`stop_cosod_web.ps1`
- 修改文件：`stop_cosod_web.bat`
- 修改目的：让用户下次能直接打开并使用多图共显著识别前后端功能。
- 修改内容摘要：启动脚本使用 `D:\Anaconda\Anaconda\envs\scosparc\python.exe` 启动 `cosod_backend.py`，检查 `/api/health`，写入 `web_backend.pid`，并打开 `http://127.0.0.1:8765/code.html`；停止脚本优先读取 PID 文件停止服务，必要时按端口查找 Python 进程停止。
- 修改时间：2026-06-09 19:02:15
- 验证情况：执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\start_cosod_web.ps1` 成功，输出 `CoSOD web app is ready: http://127.0.0.1:8765/code.html`；未触发模型推理。

### 修改前

```text
缺少一键启动和停止脚本；需要手动输入后端启动命令并打开浏览器。
```

### 修改后

```powershell
# start_cosod_web.ps1
$python = 'D:\Anaconda\Anaconda\envs\scosparc\python.exe'
...
$proc = Start-Process `
    -FilePath $python `
    -ArgumentList @('cosod_backend.py', '--host', $hostName, '--port', [string]$port) `
    -WorkingDirectory $root `
    -RedirectStandardOutput $stdoutLog `
    -RedirectStandardError $stderrLog `
    -WindowStyle Hidden `
    -PassThru
...
Start-Process $url
```

```bat
@echo off
powershell -NoProfile -ExecutionPolicy Bypass -File "%~dp0start_cosod_web.ps1"
pause
```

## 20:08:30 上传 GitHub 仓库配置与说明更新

- 修改文件：`.gitignore`
- 修改文件：`.gitattributes`
- 修改文件：`README.md`
- 修改目的：将项目上传到 `CK-NOMI/RCPR-Identify`，同时按要求排除 `datasets/` 和 `predictions/`，避免上传训练数据和预测结果；权重只保留当前推理实际使用项。
- 修改内容摘要：新增 Git 忽略规则排除数据集、预测输出、Web 临时输出和其它历史权重；新增 Git LFS 规则管理 343MB 的 DINO 预训练权重；更新 README，说明项目功能、启动方式、未上传目录和保留权重。
- 修改时间：2026-06-09 20:08:30
- 验证情况：`git diff --cached --name-only | Select-String -Pattern '^datasets/|^predictions/'` 无输出；`git lfs ls-files` 显示 `models/dino_vitbase8_pretrain.pth`；`git push origin main` 成功，远端 `main` 为 `d4e107c4a0893bc5a758b5791bd6ad1e1ce19a26`。

### 修改前

```text
仓库没有排除 datasets/ 和 predictions/ 的明确规则。
大于 GitHub 普通文件限制的 models/dino_vitbase8_pretrain.pth 未配置 Git LFS。
README 仍是目标仓库初始说明，未说明本项目运行方式、数据目录策略和保留权重。
```

### 修改后

```gitignore
datasets/
predictions/
web_runs/
web_outputs/
web_logs/
aqua20_logs/

*.pt
*.pth
!models/dino_vitbase8_pretrain.pth
!checkpoints/baseline运行出的checkpoints/model_combo_base8-136_0.7291838924090067.pt
```

```gitattributes
models/dino_vitbase8_pretrain.pth filter=lfs diff=lfs merge=lfs -text
```

```text
README 已说明：
- 项目是多图共显著物体识别 Web 演示。
- 通过 start_cosod_web.bat 启动，访问 http://127.0.0.1:8765/code.html。
- datasets/ 与 predictions/ 不上传；网页上传图片会走临时目录推理。
- 当前保留 checkpoint 为 model_combo_base8-136_0.7291838924090067.pt。
```
