# 关键代码讲解

本文档用于说明 `C:\COSOD\SCoSPARC-main提取版` 中论文实验代码的核心实现，尤其是 `RCPR / ACRE / RTG-TopK / RPF` 这条主链路。本文档不是泛泛描述，而是直接结合实际代码进行逐段讲解。

## 1. 代码结构总览

当前论文实验的关键代码主要集中在以下文件：

- `test.py`：测试入口，负责解析参数、加载模型、打开或关闭 ACRE / TopK / RPF 等开关。
- `models.py`：模型主体，包含 Stage-1 预测、Stage-2 ACRE 原型构造、RPF 二轮细化。
- `rcpr.py`：论文里最关键的鲁棒共识原型估计模块，负责从 token 特征和初始权重中估计共显著原型。
- `dataset.py`：数据集读取。
- `utils.py` / `util.py`：特征提取、可视化、日志、工具函数。
- `eval_metrics.py`：评测脚本。

其中，论文最核心的推理主链可以概括为：

1. `test.py` 设置实验配置。
2. `models.py` 先做 Stage-1 预测，得到初始前景图与 patch 权重。
3. `models.py` 调用 `rcpr.py` 的 `RCPRConsensusEstimator`，计算 ACRE 共识原型。
4. 若启用 TopK，则在 `rcpr.py` 内部对权重执行 TopK 增强。
5. 若启用 RPF round2，则 `models.py` 再基于第一轮掩码构造 `w1`，再次调用 `rcpr.py` 得到第二轮原型 `p2`。
6. 最终输出预测掩码并保存到 `predictions/`。

---

## 2. 测试入口 `test.py`

### 2.1 核心配置代码

```python
model.rcpr.topk_mode = str(args.topk_mode).lower()
model.stage2_rpf_rounds = int(args.rpf_rounds)
...
model.stage2_tau_sim01 = float(args.tau1_sim)
print(f"[Stage2TopK] ratio={model.rcpr.topk_ratio:.3f} mode={model.rcpr.topk_mode} enabled={model.rcpr.topk_ratio>0} (ACRE inference-only)")
print(f"[TOPK_RTG_CFG] mode={model.rcpr.topk_mode} alpha={model.rcpr.topk_res_alpha:.3f} ...")
print(f"[RPF] rounds={model.stage2_rpf_rounds}")
print(f"[ACRE_CALIB] tau1={float(args.tau1_sim):.3f} score_norm=none enabled={acre_calib_enabled}")
```

### 2.2 作用说明

这部分代码的职责不是做特征计算，而是把实验参数传给模型对象：

- `tau1_sim`：第一轮区域保留阈值。
- `topk_mode`：TopK 方式，`legacy` 或 `rtg`。
- `rpf_rounds`：是否启用第二轮反馈。
- `topk_ratio`：TopK 保留比例。
- `topk_res_alpha` / `topk_conf_gate` / `topk_mass_min` / `topk_delta_th`：RTG-TopK 的控制参数。

也就是说，`test.py` 决定的是“本次实验采用哪一种推理策略”，真正的算法计算发生在 `models.py` 和 `rcpr.py` 中。

---

## 3. 模型主干 `models.py`

## 3.1 RCPR 模块注册

```python
from rcpr import RCPRConsensusEstimator
...
self.rcpr = RCPRConsensusEstimator(iters=2, beta=0.6, gamma=2.0).cuda()
```

### 讲解

这里把 `RCPRConsensusEstimator` 挂到 `SCoSPARC` 模型内部。它是一个独立的 `nn.Module`，因此后续可以像普通网络层一样调用：

```python
avg_embeds, _, _ = self.rcpr(feats_tok, w0, apply_topk=(mode == 'test'), conf_vec=conf_vec_topk)
```

其中：

- `feats_tok`：patch token 特征，形状 `[B, HW, C]`。
- `w0`：第一轮前景权重，形状 `[B, HW]`。
- `avg_embeds`：RCPR 输出的共识原型 `proto`。

---

## 3.2 Stage-1 生成初始前景权重

```python
self_attn_maps, patch_toks_group = self_attention_module2(x,self.patch_size2,model_dino)
...
cross_attn_weights = self.encoder_attn(patch_toks2)
...
fg_wts = cross_attn_weights_reshaped.reshape(x.size()[0],self.num_patches2*self.num_patches2).unsqueeze(1)
```

### 讲解

这一段生成的是第一阶段的前景热力信息：

- `patch_toks_group` / `patch_toks2`：每张图的 patch 特征。
- `cross_attn_weights`：跨图注意力得到的显著性权重。
- `fg_wts`：后续给 `RCPR` 使用的初始前景权重来源。

这一步的输出不是最终掩码，而是一个“像素/patch 级置信度图”。

---

## 3.3 自适应阈值与初始二值掩码

```python
for j in range(len(patch_toks2)):
    th_map = preds_fin[j].clone()
    th_map = F.interpolate(th_map.unsqueeze(0), [28, 28], mode='bilinear', align_corners=True)
    th_map = th_map.reshape(-1)
    valid_mask = th_map >= th_val
    if bool(valid_mask.any().item()):
        avg_conf = th_map[valid_mask].mean()
        conf_vec_topk.append(avg_conf)
    else:
        avg_conf = torch.tensor(1.0, device=th_map.device, dtype=th_map.dtype)
        conf_vec_topk.append(torch.tensor(float('nan'), device=th_map.device, dtype=th_map.dtype))
    avg_conf_vals.append(avg_conf)

...
sel_th = th0 + alpha_c*((1-avg_tot_conf) - bm_bar)

for j in range(len(patch_toks2)):
    preds_fin[j][preds_fin[j] >= sel_th] = 1
    preds_fin[j][preds_fin[j] < sel_th] = 0
```

### 讲解

这里做了两件事：

1. 估计每张图的平均前景置信度 `avg_conf`，为后续 TopK 门控提供依据。
2. 用自适应阈值 `sel_th` 把连续热力图转成第一轮二值前景图 `M1`。

这里的 `conf_vec_topk` 很重要，它会作为 `RCPR` 中 RTG-TopK 的输入之一。

---

## 3.4 第一轮 ACRE 原型估计

```python
if self.stage2_use_acre:
    feats_tok = pat_tok
    w0 = fg_wts.squeeze(1) * fg_wts_masked.squeeze(1)
    avg_embeds, _, _ = self.rcpr(feats_tok, w0, apply_topk=(mode == 'test'), conf_vec=conf_vec_topk)
else:
    avg_embeds = torch.mean(fg_embeds,0)
```

### 讲解

这是论文主创新链最关键的一句。

- `pat_tok`：所有 patch token 特征。
- `fg_wts_masked`：第一轮阈值后的硬前景掩码。
- `w0 = fg_wts * fg_wts_masked`：把连续权重与二值前景区域结合，得到初始支持权重。
- `self.rcpr(...)`：进入 ACRE / RCPR 模块，输出全组共享的前景原型 `avg_embeds`。

如果不开 ACRE，就退化成简单平均原型；如果开 ACRE，就走鲁棒共识估计。

---

## 3.5 RPF 第二轮反馈

```python
use_rpf_round2 = self.stage2_use_acre and int(getattr(self, 'stage2_rpf_rounds', 1)) == 2
...
m1_flat_batch = torch.stack(round1_m1_flat, dim=0)
if lam <= 1e-12:
    m_mix = m1_flat_batch
else:
    m1_soft_batch = torch.stack(round1_m1_soft_flat, dim=0)
    m_mix = (1.0 - lam) * m1_flat_batch + lam * m1_soft_batch
w1 = w0 * m_mix
p2, _, _ = self.rcpr(pat_tok, w1, apply_topk=(mode == 'test'), conf_vec=conf_vec_topk)
```

### 讲解

这一段表示：

- 第一轮掩码 `M1` 不是终点，还可以反过来约束第二轮原型估计。
- `w1 = w0 * m_mix`：把第一轮的前景支持进一步注入到权重中。
- `p2 = self.rcpr(...)`：再次调用 RCPR，得到第二轮原型 `p2`。

其中：

- `lam = 0` 时是纯硬反馈 `M_hard`。
- `lam > 0` 时是软硬混合反馈 `M_mix = (1-λ)M_hard + λM_soft`。

所以 RPF 的本质不是重新训练，而是“在推理阶段利用第一轮掩码做二次原型校正”。

---

# 4. `rcpr.py` 全量代码讲解

下面开始对 `rcpr.py` 做完整讲解。该文件中所有核心模块都会给出代码和解释。

---

## 4.1 类定义与初始化

```python
class RCPRConsensusEstimator(nn.Module):
    """
    ACRE-only robust consensus prototype estimator for CoSOD Stage-2.
    Inputs:
      feats: [B, HW, C]
      w0:    [B, HW] in [0, 1]
    Outputs:
      proto: [C]
      agree: [B, HW] in [0, 1]
      w:     [B, HW] normalized weights
    """

    def __init__(self, iters=2, beta=0.6, gamma=2.0, eps=1e-6):
        super().__init__()
        self.iters = iters
        self.beta = beta
        self.gamma = gamma
        self.eps = eps
        # TopK v2: inference-only enhancement, default off.
        self.topk_ratio = 0.0
        self.topk_mode = 'legacy'
        self.topk_res_alpha = 0.20
        self.topk_conf_gate = 0.58
        self.topk_mass_min = 0.55
        self.topk_delta_th = 0.035
        self._printed_topk_shape = False
        self._printed_topk_once = False
        self._printed_topk_fallback = False
        self._printed_topk_rtg_once = False
```

### 讲解

这个类的作用是：

- 输入一组图像的 token 特征 `feats`；
- 再输入每个 token 的初始支持权重 `w0`；
- 输出一个全组共享的共识原型 `proto`，以及经过一致性重加权后的最终权重 `w`。

### 变量说明

- `iters`：RCPR 迭代次数。当前实现为 2。
- `beta`：新权重与旧权重的融合系数。
- `gamma`：agreement 的幂指数，用来增强高一致性 token 的作用。
- `eps`：数值稳定项，防止除零。
- `topk_ratio`：TopK 保留比例。
- `topk_mode`：`legacy` 表示硬 TopK，`rtg` 表示风险控制 TopK。
- `topk_res_alpha`：RTG 中残差混合强度。
- `topk_conf_gate`：低置信度门控阈值。
- `topk_mass_min`：TopK 后最小保留质量约束。
- `topk_delta_th`：TopK 原型漂移阈值。

这一段本质上是在定义 RCPR 的算法超参数和日志开关。

---

## 4.2 余弦相似度计算 `_cos_sim_tokens_to_proto`

```python
@staticmethod
def _cos_sim_tokens_to_proto(feats, proto, eps=1e-6):
    feats_n = F.normalize(feats, dim=-1, eps=eps)
    proto_n = F.normalize(proto, dim=-1, eps=eps)
    return (feats_n * proto_n.view(1, 1, -1)).sum(dim=-1)
```

### 讲解

这是 RCPR 的基础相似度函数。

它的功能是：计算每个 token 与当前原型 `proto` 的余弦相似度。

输入输出：

- 输入 `feats`：形状 `[B, HW, C]`。
- 输入 `proto`：形状 `[C]`。
- 输出：形状 `[B, HW]`，表示每个 token 和原型的相似度。

这一步的意义是：

- 原型越能代表真正的共显著前景，那么前景 token 与原型的相似度越高；
- 后续就可以依据该相似度对初始权重 `w0` 做共识重加权。

---

## 4.3 传统硬 TopK `_apply_topk_on_weights`

```python
def _apply_topk_on_weights(self, w, ratio):
    """
    Apply row-wise TopK filtering on w.
    Inputs:
      w: [B, HW] normalized weights.
      ratio: scalar in [0, 1].
    Returns:
      w_topk: [B, HW], row-normalized.
    """
    eps = self.eps
    ratio = 0.0 if ratio is None else float(ratio)
    ratio = max(0.0, min(1.0, ratio))
    if ratio <= 0.0:
        return w

    if w.dim() != 2:
        raise ValueError(f"TopK expects w as [B,HW], got {tuple(w.shape)}")

    bsz, hw = w.shape
    k = int(math.ceil(float(ratio) * float(hw)))
    k = max(1, min(k, hw))

    if not self._printed_topk_once:
        print(f"[Stage2TopK] enabled=1 ratio={float(ratio):.3f} hw={hw} k={k}")
        self._printed_topk_once = True

    if k >= hw:
        return w

    _, idx = torch.topk(w, k, dim=1, largest=True, sorted=False)
    mask = torch.zeros_like(w)
    mask.scatter_(1, idx, 1.0)
    w_masked = w * mask

    denom = w_masked.sum(dim=1, keepdim=True)
    w_norm = w / (w.sum(dim=1, keepdim=True) + eps)
    need_fallback = denom <= eps

    if bool(need_fallback.any().item()) and (not self._printed_topk_fallback):
        fb_rows = int(need_fallback.sum().item())
        print(f"[TopKFallback] rows={fb_rows}/{bsz} denom<=eps -> fallback to original normalized w")
        self._printed_topk_fallback = True

    w_topk = torch.where(need_fallback, w_norm, w_masked / (denom + eps))
    return w_topk
```

### 讲解

这是最传统的 TopK：

1. 对每一行权重 `w[i]` 取前 `k` 个最大值；
2. 其他位置清零；
3. 再重新归一化。

优点：

- 简单直接；
- 可以过滤低权重噪声 token。

缺点：

- 过于激进，容易把有用信息全部裁掉；
- 跨数据集稳定性较差。

因此后续代码又实现了更稳健的 `RTG-TopK`。

---

## 4.4 每图原型计算 `_compute_proto_per_group`

```python
def _compute_proto_per_group(self, feats, w):
    eps = self.eps
    denom = w.sum(dim=1, keepdim=True).clamp(min=eps)
    return (w.unsqueeze(-1) * feats).sum(dim=1) / denom
```

### 讲解

这个函数的功能很单一：

- 依据每张图自己的权重 `w[i]`，计算每张图自己的局部原型。

输入输出：

- 输入 `feats`：`[B, HW, C]`
- 输入 `w`：`[B, HW]`
- 输出：`[B, C]`

之所以需要它，是因为 RTG-TopK 要比较：

- 原始权重下的每图原型 `p_full`
- TopK 后的每图原型 `p_topk`

二者差异过大时说明 TopK 裁剪过猛，需要回退。

---

## 4.5 风险控制 TopK `_apply_topk_rtg`

```python
def _apply_topk_rtg(self, feats, w, ratio, conf_vec=None):
    """
    RTG-TopK (risk-controlled TopK):
      1) TopK on each image/group row
      2) Residual convex blending with full weights
      3) Per-image gating by confidence, retained mass and proto drift
    """
    eps = self.eps
    ratio = 0.0 if ratio is None else float(ratio)
    ratio = max(0.0, min(1.0, ratio))
    if ratio <= 0.0:
        return w, None

    if w.dim() != 2:
        raise ValueError(f"TopK expects w as [B,HW], got {tuple(w.shape)}")

    bsz, hw = w.shape
    k = int(math.ceil(float(ratio) * float(hw)))
    k = max(1, min(k, hw))
    if not self._printed_topk_once:
        print(f"[Stage2TopK] enabled=1 mode=rtg ratio={float(ratio):.3f} hw={hw} k={k}")
        self._printed_topk_once = True

    w_norm = w / (w.sum(dim=1, keepdim=True) + eps)
    if k >= hw:
        delta = torch.zeros((bsz,), device=w.device, dtype=w.dtype)
        stats = {
            'applied_rate': 0.0,
            'applied': 0,
            'total': int(bsz),
            'delta_mean': 0.0,
            'delta_p95': 0.0,
            'conf_invalid': int(bsz if conf_vec is None else 0),
            'mass_fail': 0,
            'delta_fail': 0,
        }
        return w_norm, stats

    _, idx = torch.topk(w_norm, k, dim=1, largest=True, sorted=False)
    mask = torch.zeros_like(w_norm)
    mask.scatter_(1, idx, 1.0)
    w_masked = w_norm * mask
    mass_keep = w_masked.sum(dim=1)

    denom = w_masked.sum(dim=1, keepdim=True)
    need_fallback = (denom <= eps).squeeze(1)
    if bool(need_fallback.any().item()) and (not self._printed_topk_fallback):
        fb_rows = int(need_fallback.sum().item())
        print(f"[TopKFallback] rows={fb_rows}/{bsz} denom<=eps -> fallback to original normalized w")
        self._printed_topk_fallback = True

    w_topk = torch.where(need_fallback.unsqueeze(1), w_norm, w_masked / (denom + eps))
    alpha = max(0.0, min(1.0, float(self.topk_res_alpha)))
    w_res = (1.0 - alpha) * w_topk + alpha * w_norm
    w_res = w_res / (w_res.sum(dim=1, keepdim=True) + eps)

    p_full = self._compute_proto_per_group(feats, w_norm)
    p_topk = self._compute_proto_per_group(feats, w_topk)
    delta = (1.0 - F.cosine_similarity(p_full, p_topk, dim=1, eps=eps)).clamp(min=0.0, max=2.0)

    if conf_vec is None:
        conf = torch.ones((bsz,), device=w.device, dtype=w.dtype)
        conf_invalid = torch.ones((bsz,), device=w.device, dtype=torch.bool)
    else:
        conf = conf_vec.reshape(-1).to(device=w.device, dtype=w.dtype)
        if conf.numel() != bsz:
            conf = torch.ones((bsz,), device=w.device, dtype=w.dtype)
            conf_invalid = torch.ones((bsz,), device=w.device, dtype=torch.bool)
        else:
            conf_invalid = torch.isnan(conf) | torch.isinf(conf)
            conf = torch.where(conf_invalid, torch.ones_like(conf), conf)

    conf_gate = max(0.0, min(1.0, float(self.topk_conf_gate)))
    mass_min = max(0.0, min(1.0, float(self.topk_mass_min)))
    delta_th = max(0.0, float(self.topk_delta_th))
    conf_pass = conf < conf_gate
    mass_pass = mass_keep >= mass_min
    delta_pass = delta <= delta_th
    use_topk = conf_pass & mass_pass & delta_pass & (~need_fallback)

    w_final = torch.where(use_topk.unsqueeze(1), w_res, w_norm)
    w_final = w_final / (w_final.sum(dim=1, keepdim=True) + eps)

    if bsz > 0:
        delta_sorted, _ = torch.sort(delta.detach())
        p95_idx = int(math.ceil(0.95 * bsz)) - 1
        p95_idx = max(0, min(p95_idx, bsz - 1))
        delta_p95 = float(delta_sorted[p95_idx].item())
        delta_mean = float(delta.mean().item())
    else:
        delta_p95 = 0.0
        delta_mean = 0.0

    stats = {
        'applied_rate': float(use_topk.float().mean().item()) if bsz > 0 else 0.0,
        'applied': int(use_topk.sum().item()),
        'total': int(bsz),
        'delta_mean': delta_mean,
        'delta_p95': delta_p95,
        'conf_invalid': int(conf_invalid.sum().item()),
        'mass_fail': int((~mass_pass).sum().item()),
        'delta_fail': int((~delta_pass).sum().item()),
    }
    return w_final, stats
```

### 讲解

这就是当前项目里改进后的 TopK 核心实现。

它不是简单硬裁剪，而是三步：

#### 第一步：先做普通 TopK

- 保留每图前 `k` 个高权重 token。
- 得到 `w_topk`。

#### 第二步：残差凸组合

```python
w_res = (1.0 - alpha) * w_topk + alpha * w_norm
```

这里不是把非 TopK token 全部扔掉，而是保留一部分原始权重 `w_norm`，避免信息损失过大。

#### 第三步：风险门控

是否真正启用 TopK，不由 ratio 单独决定，而由三种条件共同决定：

```python
conf_pass = conf < conf_gate
mass_pass = mass_keep >= mass_min
delta_pass = delta <= delta_th
use_topk = conf_pass & mass_pass & delta_pass & (~need_fallback)
```

含义分别是：

- `conf_pass`：该图当前置信度不高，说明有必要修正。
- `mass_pass`：TopK 后保留的权重质量不能太小，否则裁得太狠。
- `delta_pass`：TopK 前后原型漂移不能太大，否则说明 TopK 改坏了表示。

只有同时满足这三条，才真正采用 `w_res`；否则回退到原始 `w_norm`。

### 输出统计量的意义

- `applied_rate`：TopK 真正生效的图像比例。
- `delta_mean`：TopK 前后平均原型漂移。
- `delta_p95`：原型漂移的 95 分位数。
- `mass_fail`：因为保留质量不足而失败的图像数。
- `delta_fail`：因为原型漂移过大而失败的图像数。

这些日志字段不是装饰，而是论文分析 TopK 是否稳定的重要证据。

---

## 4.6 RCPR 主前向传播 `forward`

```python
def forward(self, feats, w0, apply_topk=False, conf_vec=None):
    eps = self.eps
    w0 = w0.clamp(min=0.0)
    bsz, hw, _ = feats.shape
    w0_sum = w0.sum(dim=1, keepdim=True)
    fallback_mask = (w0_sum < eps)
    uniform_w = torch.full_like(w0, 1.0 / max(hw, 1))
    w = w0 / (w0_sum + eps)
    w = torch.where(fallback_mask, uniform_w, w)

    if apply_topk and (not self._printed_topk_shape):
        print(f"[Stage2TopKShape] feats_shape={tuple(feats.shape)} w0_shape={tuple(w0.shape)}")
        self._printed_topk_shape = True
    if apply_topk:
        if w.shape != w0.shape:
            raise ValueError(f"TopK expects w.shape==w0.shape, got {tuple(w.shape)} vs {tuple(w0.shape)}")

    proto = feats.mean(dim=(0, 1))
    agree = torch.full_like(w, 0.5)
    for _ in range(max(1, self.iters)):
        if bool(fallback_mask.squeeze(1).all().item()):
            proto = feats.mean(dim=(0, 1))
        else:
            denom = w.sum(dim=(0, 1)) + eps
            proto = (w.unsqueeze(-1) * feats).sum(dim=(0, 1)) / denom
        cos_raw = self._cos_sim_tokens_to_proto(feats, proto, eps=eps)
        agree = ((cos_raw + 1.0) * 0.5).clamp(0.0, 1.0)
        agree = torch.where(fallback_mask, torch.full_like(agree, 0.5), agree)
        w_new = w0 * (agree ** self.gamma)
        w_new = w_new / (w_new.sum(dim=1, keepdim=True) + eps)
        w_new = torch.where(fallback_mask, uniform_w, w_new)
        w = (1.0 - self.beta) * w + self.beta * w_new
        w = w / (w.sum(dim=1, keepdim=True) + eps)
        w = torch.where(fallback_mask, uniform_w, w)

    if apply_topk and (self.topk_ratio > 0.0):
        topk_mode = str(getattr(self, 'topk_mode', 'legacy')).lower()
        if topk_mode == 'rtg':
            w, rtg_stats = self._apply_topk_rtg(feats, w, self.topk_ratio, conf_vec=conf_vec)
            if (rtg_stats is not None) and (not self._printed_topk_rtg_once):
                print(
                    f"[TOPK_RTG] applied_rate={rtg_stats['applied_rate']:.3f} "
                    f"applied={rtg_stats['applied']}/{rtg_stats['total']} "
                    f"delta_mean={rtg_stats['delta_mean']:.4f} delta_p95={rtg_stats['delta_p95']:.4f} "
                    f"conf_invalid={rtg_stats['conf_invalid']} "
                    f"mass_fail={rtg_stats['mass_fail']} delta_fail={rtg_stats['delta_fail']}"
                )
                self._printed_topk_rtg_once = True
        else:
            w = self._apply_topk_on_weights(w, self.topk_ratio)
        denom = w.sum(dim=(0, 1)) + eps
        proto = (w.unsqueeze(-1) * feats).sum(dim=(0, 1)) / denom

    return proto, agree, w
```

### 讲解

这是整份 `rcpr.py` 的核心主循环。

可以分成四步理解。

### 第一步：初始化权重

```python
w = w0 / (w0_sum + eps)
```

先把初始权重 `w0` 做归一化，得到每张图内部的 token 权重分布。

如果某张图 `w0` 全空：

```python
fallback_mask = (w0_sum < eps)
uniform_w = torch.full_like(w0, 1.0 / max(hw, 1))
```

则直接退回均匀权重，保证算法稳定。

### 第二步：ACRE / RCPR 迭代更新

```python
for _ in range(max(1, self.iters)):
    proto = (w.unsqueeze(-1) * feats).sum(dim=(0, 1)) / denom
    cos_raw = self._cos_sim_tokens_to_proto(feats, proto, eps=eps)
    agree = ((cos_raw + 1.0) * 0.5).clamp(0.0, 1.0)
    w_new = w0 * (agree ** self.gamma)
    w = (1.0 - self.beta) * w + self.beta * w_new
```

这是 RCPR 的数学核心：

1. 用当前权重 `w` 计算全组共识原型 `proto`。
2. 计算每个 token 与 `proto` 的 agreement。
3. 用 `agreement^gamma` 去强化高一致性 token。
4. 得到新的权重 `w_new`。
5. 再用 `beta` 做平滑更新，避免抖动。

这就是“共识原型估计 + 一致性重加权”的完整闭环。

### 第三步：可选 TopK 增强

```python
if apply_topk and (self.topk_ratio > 0.0):
```

只有在测试阶段、并且 `topk_ratio > 0` 时，TopK 才会介入。

这说明：

- ACRE / RCPR 是主干；
- TopK 是附加推理增强；
- TopK 并不参与训练。

### 第四步：TopK 后重新计算最终原型

```python
denom = w.sum(dim=(0, 1)) + eps
proto = (w.unsqueeze(-1) * feats).sum(dim=(0, 1)) / denom
```

如果 TopK 修改了最终权重 `w`，那么共识原型 `proto` 也必须随之重算。这个 `proto` 才是最终给 `models.py` 使用的输出。

---

# 5. RCPR 在整条论文推理链中的位置

从实现角度看，RCPR 不是孤立模块，而是插在整个推理链的中间：

1. Stage-1 先产生初始显著图。
2. 根据显著图构造 `w0`。
3. `RCPR(feats, w0)` 生成共识原型 `p1`。
4. 用 `p1` 对区域进行筛选，得到第一轮结果 `M1`。
5. 如果启用 RPF，则再用 `M1` 构造 `w1`。
6. `RCPR(feats, w1)` 生成第二轮原型 `p2`。
7. 用 `p2` 重新评分区域，得到最终结果。

因此：

- ACRE / RCPR 决定“什么样的 token 特征才算真正共显著”；
- TopK 决定“在推理中是否要进一步压制噪声 token”；
- RPF 决定“是否让第一轮掩码反过来再优化一次原型”。

---

# 6. 关键结论

## 6.1 从代码结构上看

当前论文实验主创新并不在 `test.py`，而在两层：

- `models.py`：定义整条 Stage-2 推理流程；
- `rcpr.py`：实现共识原型估计与 TopK 风险控制。

## 6.2 从算法作用上看

- `RCPR / ACRE`：负责构建稳定的组级前景原型。
- `RTG-TopK`：负责在不破坏原型的前提下抑制噪声 token。
- `RPF`：负责利用第一轮掩码做反馈式原型修正。

## 6.3 从论文表达上看

如果在报告或论文中需要突出最核心代码，应优先展示：

1. `models.py` 中 `self.rcpr(feats_tok, w0, ...)` 这一段；
2. `rcpr.py` 中 `forward()` 的原型-一致性迭代；
3. `rcpr.py` 中 `_apply_topk_rtg()` 的风险门控逻辑；
4. `models.py` 中 `w1 = w0 * m_mix` 与 `p2 = self.rcpr(...)` 的 RPF 二轮反馈。

---

# 7. 建议截图的代码片段

为了在报告中展示“实验代码内容”，建议优先截图以下四段：

- `models.py`：Stage-1 到 `w0` 的构造。
- `models.py`：`avg_embeds, _, _ = self.rcpr(...)` 这一段。
- `rcpr.py`：`forward()` 主循环。
- `rcpr.py`：`_apply_topk_rtg()` 中 `use_topk = conf_pass & mass_pass & delta_pass & (~need_fallback)` 这一段。

这四段最能体现你的论文算法主线。
